import { FormatCustoPipe } from './format-custo.pipe';

describe('FormatCustoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCustoPipe();
    expect(pipe).toBeTruthy();
  });
});
